[package]
name = "player-fixture"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
pinocchio        = "0.10"
pinocchio-system = "0.5"

[profile.release]
opt-level = 3
overflow-checks = true
